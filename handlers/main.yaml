---
# handlers file for node_exporter

- name: restart node_exporter
  systemd:
    name: node_exporter
    state: restarted
    daemon_reload: yes
  tags:
    - prometheus
    - node_exporter

- name: systemd reload
  systemd:
    daemon_reload: yes
  tags:
    - prometheus
    - node_exporter
...
